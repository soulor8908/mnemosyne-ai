# Mnemosyne 环境变量示例
# 复制为 .env.local 并填入真实值

# 平台 Trial 模式的 AI provider keys（可选，BYOK 用户不需要）
DEEPSEEK_API_KEY=
GLM_API_KEY=

# 服务端根密钥（32 字节 base64，openssl rand -base64 32 生成）
MASTER_KEY=

# 默认 AI provider
AI_PROVIDER=deepseek

# 应用 URL
APP_URL=http://localhost:3000
